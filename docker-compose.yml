services:
  siga-solid:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: siga-solid
    # network_mode: none
    volumes:
      - ./src:/app/src
    command: >
      sh -c "javac -d bin src/siga/*.java && java -cp bin siga.Main"
